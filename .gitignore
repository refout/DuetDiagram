## Build output
bin/
obj/
artifacts/
*.user
*.suo
*.userprefs

## IDE
.vs/
.idea/
.vscode/
*.DotSettings.user

## Test / coverage
TestResults/
*.coverage
*.coveragexml
coverage*.json
coverage*.xml

# 快照比对失败时留下的实际结果。它是给人看的中间产物，不是期望值——
# 核对过之后要么改代码、要么把它提升成正式的 .txt，不要把它提交进来。
*.received.txt

## BenchmarkDotNet
BenchmarkDotNet.Artifacts/

## Rider / VS build caches
*.nupkg
*.snupkg

## Local secrets and machine state
secrets/
*.local.json

## OS
Thumbs.db
Desktop.ini
.DS_Store

## NuGet
packages/
.packages/

# 编辑器插件的本地统计产物。与 reports/loc.md 重复，不该进仓库。
.VSCodeCounter/

# 盲评清单与对照表。由 reports/raw 经 `listings` 命令逐字节可复现，
# 不是证据而是评分时的工作材料；把 key.json 的副本留在仓库里还会多一份泄露面。
# 结论留在 reports/compare-blind/parse-report.md，那个进仓库。
reports/compare-blind/rater.md
reports/compare-blind/items.json
reports/compare-blind/key.json

# 抽样评分表与它的对照表，理由同上。
reports/compare-blind/sample-rater.md
reports/compare-blind/sample-key.json

# 人工评分。里面既有评分者本人的判断，也有"哪一条来自哪一组"的对照，
# 不进版本库。由它算出来的结论 reports/compare-blind/agreement.md 进仓库，
# 报告里记着评分文件的指纹，对得上才认。
reports/compare-blind/human/
