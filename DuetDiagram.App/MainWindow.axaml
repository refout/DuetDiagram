<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:DuetDiagram.App.Controls"
        xmlns:vm="using:DuetDiagram.App.ViewModels"
        x:Class="DuetDiagram.App.MainWindow"
        x:DataType="vm:CanvasViewModel"
        Title="DuetDiagram"
        Width="900" Height="600">
  <DockPanel>
    <!--
      菜单栏与工具栏都停靠在顶部。它们与画布之间是"占一行"的关系，
      不像诊断面板那样浮着——浮着的工具栏会盖住画布上沿，而画布上沿正是
      用户第一眼要看的地方。
    -->
    <controls:DiagramMenuBar x:Name="MenuBarView" DockPanel.Dock="Top" />

    <controls:DiagramToolBar x:Name="ToolBarView" DockPanel.Dock="Top" />

    <!--
      状态栏。坐标、缩放与最近一次失败的提示共用一个数据上下文，不靠互相回调——
      分成两个来源的话，这一行到底谁说了算会变得含糊。
    -->
    <controls:StatusBar x:Name="StatusBarView" DockPanel.Dock="Bottom" />

    <!--
      图层面板占左侧一栏，常驻。方案 §9.1 把六个分区写成固定布局，所以它与属性面板一样
      是分栏而不是浮层——浮层的开关会改画布尺寸，而画布一改尺寸视口跟着跳。
      常驻还有一个好处：图层这件事的意义在于「一层里有谁」，随时看得见才谈得上。
    -->
    <!--
      左栏上下分两截：图层在上面，调色板在下面。调色板也是常驻分区（§9.1 的样式/调色板），
      理由与图层那条相同——「有哪些名字可用」随时看得见，令牌才填得出来。
    -->
    <Grid DockPanel.Dock="Left"
          Width="240"
          RowDefinitions="Auto,*">
      <controls:LayerPanel x:Name="LayersView" Grid.Row="0" />
      <controls:PalettePanel x:Name="PaletteView" Grid.Row="1" />
    </Grid>

    <!--
      属性面板占右侧一栏。它的数据上下文由主窗口在代码里设成属性面板的状态，
      与画布那份不是同一个对象——两边的状态各自独立，谁也不该看见对方的一半。
    -->
    <controls:PropertyPanel x:Name="PropertiesView"
                            DockPanel.Dock="Right"
                            Width="300" />

    <!--
      画布那一格里再分一次：标签栏停在画布上方，而画布与那几个浮层叠在同一格里。
      浮层不进停靠位——停靠会改变画布的尺寸，而画布一改尺寸视口就跟着变，
      开关一次面板图就跳一下。
    -->
    <DockPanel>
      <controls:PageTabs x:Name="PageTabsView" DockPanel.Dock="Top" />

      <Grid>
        <controls:DiagramCanvas x:Name="Canvas" />
        <controls:DiagnosticsPanel HorizontalAlignment="Right"
                                   VerticalAlignment="Top"
                                   Margin="12"
                                   IsVisible="{Binding Diagnostics.IsOpen}" />
        <!--
          变更边栏浮在右下角。它与诊断面板一样不进停靠位：停靠会改画布尺寸，
          而尺寸一变视口就跳，开一次边栏图就挪一下位置。
        -->
        <controls:DiffSidebar x:Name="DiffView"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Bottom"
                              Margin="12" />
        <!--
          两条提示都浮在正中间，默认收起来。它们不进停靠位：一出现就改画布尺寸的话，
          图会在"出错的那一刻"跟着跳一下，而用户正在盯着它看出错在哪。
        -->
        <controls:LayoutFailureDialog x:Name="LayoutFailureView"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsVisible="False" />
        <controls:SidecarRecoveryDialog x:Name="SidecarRecoveryView"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        IsVisible="False" />
      </Grid>
    </DockPanel>
  </DockPanel>
</Window>
