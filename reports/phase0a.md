# Phase 0a 验证结论

- 验证时间：2026-09-20
- 覆盖任务：P0-01、P0-02、P0-03、P0-04
- 待办任务：P0-05、P0-08、P0-09、P0-10

## 决策门 0 现状

| 任务 | 结论 |
|---|---|
| P0-01 NuGet 版本核查与锁定 | **绿** —— 六个主选依赖全部真实存在，版本已精确锁定 |
| P0-02 界面框架 + 绘图库最小应用 | **黄** —— 渲染链路与冷启动达标，原生发布被本机工具链阻塞 |
| P0-03 主选布局引擎约束支持 | **红** —— 主选不成立，见 `phase0a-mermaider.md` |
| P0-04 备选布局引擎对比与选型 | **黄** —— 备选可用且性能达标，但它与主选都不支持固定位置 |
| P0-05 / P0-08 / P0-09 / P0-10 | 未开始 |

**综合判定：黄。** 技术栈里有两块需要人工决策或自建，

1. 布局引擎改走备选（`Mostlylucid.Dagre` 的索引式入口），性能与坐标质量都达标。
2. 固定位置与同层约束两个候选引擎都不支持，必须自建，见 P0-10。

原生编译链接因本机缺少 C++ 工具链而无法验证，这一项在装上工具链之前不能关闭。

## P0-02 界面框架与绘图库验证

### 结论：渲染链路可用，冷启动达标，原生发布未完成

技术栈：`Avalonia` 12.1.2 + `SkiaSharp` 4.148.0。

### 实测数据

以构建出的可执行文件直接运行（进程启动到出图后退出）：

| 指标 | 实测 |
|---|---|
| 冷启动（进程启动 → 出图 → 退出） | **~700 ms**（三次：725 / 693 / 697） |
| 其中：平台就绪 | ~440 ms |
| 其中：首帧渲染 | ~215 ms |
| 绘制元素数 | 4 |
| 位图尺寸 | 900 × 600 |

框架无关的启动开销（进程创建 + 运行时初始化）约 260 毫秒，由总时长减去进程内计时得出。

目标上限是 2 秒，当前约 700 毫秒，**留有接近三倍余量**。
需要说明的是这次测量是带运行时即时编译的构建，原生编译后的启动通常更快，
所以这个数字是偏保守的。

### 已经证实的能力

1. **自绘路径可用。** 直接重写控件的渲染方法，用绘制上下文画矩形、圆角矩形、
   自定义路径（菱形与箭头三角形）以及文字，产出结果正确，见 `phase0a-selftest.png`。
2. **文字渲染正确。** 中文标签在没有额外配置的情况下正常显示，没有出现方块或缺字。
3. **原生绘图库可加载。** 直接调用它读取版本、加载字体并测量中英混排文本宽度，
   返回 `4.148.0.0`、字体 `Segoe UI`、示例文本宽度 57.0。这说明原生库被正确带起来了，
   不是只在托管层假装可用。
4. **界面声明用编译期绑定。** 已开启，这是能通过裁剪与原生编译的前提。

### 阻塞项：原生发布无法完成

```
error : Platform linker not found. Ensure you have all the required prerequisites
        documented at https://aka.ms/nativeaot-prerequisites, in particular the
        Desktop Development for C++ workload in Visual Studio.
```

失败点非常明确：**托管编译与中间代码生成都已完成，卡在最后一步的原生链接**。
本机只装了 .NET SDK，没有装 Visual Studio 的 C++ 桌面开发工作负载，
因此缺少链接器。

这意味着：

- 托管层能否通过裁剪与原生编译分析，已经得到确认（构建过程 0 警告，
  包括裁剪分析器与原生编译分析器）。
- 但"能链接成原生可执行文件并运行"这一点**尚未验证**，而它正是这个任务要回答的核心问题。
- 一个可预见的风险是：界面框架与绘图库在运行时是否依赖反射式类型解析。
  这类问题只在原生编译后才会暴露，分析器不一定能全部覆盖。

**需要安装「使用 C++ 的桌面开发」工作负载后才能关闭这一项。**

### 顺带发现的接口变化

界面框架 12 版本把位图保存的编码参数从"质量整数"改成了独立的选项类型
（`PngBitmapEncoderOptions` / `JpegBitmapEncoderOptions`）。
不带编码参数的重载已标记为过时。导出功能实现时要按新接口写。

## P0-03 布局引擎验证

详见 `phase0a-mermaider.md`。要点：

- 主选引擎在方向、间距、同层约束、复合分组、确定性、速度上都达标。
- **固定位置完全没有输入通道** —— 人工拖动的位置无法带入下一次布局。
- **只要出现交叉边，横向坐标就会爆炸**：20 个节点的图上，116 像素的合理宽度被撑到 40803 像素；
  1000 节点时达到 27.7 亿像素。

## 对后续任务的影响

1. **布局引擎改用备选。** 选型与对比数据见 `phase0a-layout.md`。
   实现时必须用索引式入口：默认入口在 1000 节点多层图上要 1204 毫秒，已经超出预算。
2. **固定位置与同层约束必须自建。** 两款候选都不支持，而它们正是"人工拖动作为布局约束反馈"
   的实现基础。缺了它们，用户的所有手动调整都会在下一次重排时丢失。见 P0-10。
3. **布局接口的输入必须包含固定位置、同层、层内顺序、对齐、相对位置。**
   其中前两项由我们自己补齐，后三项两个候选都没有入口，需要一并评估。
4. **坐标合理性要成为长期回归断言。** 主选引擎的缺陷靠读代码看不出来，
   只能靠"横向散布有上界"这类可计算的性质来防。
5. **渲染层暂不开工。** 先在 P0-10 把约束补齐方案定下来，
   否则渲染做出来会因为坐标来源不可信而返工。
6. **持续集成里加一个原生发布作业**，并用一台装了 C++ 工作负载的机器跑，
   把 P0-02 与 P0-04 里被阻塞的那一项真正关闭。

## 复现方式

```bash
# P0-03 / P0-04：两个候选引擎跑同一套判据
dotnet run --project tools/Poc/LayoutCandidates -c Release

# 只比规模与耗时，包含两个调用入口
dotnet run --project tools/Poc/LayoutCandidates -c Release -- --perf

# P0-02：界面栈自检并出图
dotnet run --project DuetDiagram.App -c Release -- --selftest --out reports/phase0a-selftest.png

# P0-02：原生发布（需要 C++ 桌面开发工作负载）
dotnet publish DuetDiagram.App/DuetDiagram.App.csproj -c Release
```
