# 任务 DAG

面向 coding agent 的任务描述。**单任务单 PR，不超过 500 行变更。**
人类只写任务、审 PR、做决策。

## 模板

```yaml
id: P0-02                     # 任务 ID，同时是分支名与 PR 标题前缀
title: 一句话说清交付物
phase: 0a                     # 0a / 0b / 1 / 2 / 3 / 4 / 5
status: pending               # pending / in-progress / done / blocked
depends_on: [P0-01]           # 这些任务必须已合并到 main
estimated_loc: 150
files:
  create: [路径...]            # 新建
  modify: [路径...]            # 修改（已存在的）
  test:   [路径...]            # 测试文件
acceptance:
  - command: dotnet build ...
    expect: exit 0
constraints:                  # 本任务不得违反的约束
  - ...
context:                      # 自包含上下文：要读的文档与代码
  - ...
```

## DAG（Phase 0a）

```
P0-01 NuGet 版本核查+锁定 ──┬── P0-02 Avalonia+SkiaSharp AOT 最小应用
                            ├── P0-03 Mermaider Sugiyama 约束验证
                            ├── P0-04 Mostlylucid.Dagre 复合图+AOT
                            ├── P0-05 ModelContextProtocol 传输+initialize
                            └── P0-09 MEAI+MCP 工具共用 PoC

P0-08 LLM 客户端（从 Day 1 并行，不阻塞）
```

**决策门 0** 在 Phase 0a 结束时判定：全绿（按计划）/ 黄（走备选）/ 红（人类决策）。

## Agent 执行流程

1. 读任务 YAML 与它指向的 `context` 文档
2. 检查 `depends_on` 全部已合并到 `main`
3. 从 `main` 拉分支，分支名 `{id}-{slug}`
4. 实现代码 + 测试
5. 本地跑 `acceptance` 全部通过
6. 提 PR：变更摘要 + 验收输出 + 未决问题 + 依赖解锁清单
7. 人类 review（只审不改）
8. 合并后下游任务自动解锁

## 冲突与失败

- 两个 agent 改同一文件：后合并者负责 rebase
- 冲突超过 50 行：人类仲裁
- 失败自动重试 3 次（每次从干净分支），3 次后标记 `blocked`

## 已完成

**进度以各 YAML 里的 `status` 为准，不要在这里另维护一份清单。**
两份清单必然分叉，而分叉之后没人知道该信哪一份——本文件原先就只写着
「P1-01 / P1-02 / P1-03 已落地」，那时已经有十四个任务完成了。

当前（2026-09-22）：Phase 0a / 0b 除 P0-02 标 `blocked` 外全部 `done`；
Phase 1 除 P1-14 外全部 `done`——P1-01 ~ P1-13、P1-15 ~ P1-18 已落地。
Phase 2 全部落地：P2-01 ~ P2-13 都是 `done`，其中 **P2-13 与 P1-14 一样，装置齐了缺的是人**。
Phase 3 已开工：P3-01 ~ P3-03 落地（断边命令 + 命令清单门禁；布局三条与调色板三条命令；
组合三条与图层三条命令），P3-04 ~ P3-16 是 `pending`。

**两个决策门都还开着，而且卡在同一件事上。** 决策门 1（DSL 去留）与决策门 2（布局引擎主选）
都要读 `reports/compare-blind/` 下的人工评分，而那份评分要人来打。所以两个门都不是
「还没做」，是「做不了」——编码 agent 判不了自己写的东西好不好用。
**Phase 3 不等这两个门。** 它做的是 MCP 与工具层，落在 IR 与命令层之上，
换掉哪个文本格式都一样；唯一沾边的是 `diagram_export` 的 DSL 那一路，
DSL 被判掉时把那条路摘掉即可，不影响别的任务。

**P1-14 的装置全齐了，缺的是人。** `tools/CompareHarness` 七条命令
（`generate` / `listings` / `semantic` / `score` / `verify` / `sample` / `agreement`）
能自动算的都算了。剩下三步：判 60 份抽样把谓词校一遍、再决定要不要评全量 150 份、
最后填决策门 1。**这三步编码 agent 做不了**，理由见 `reports/compare-blind/conclusion.md`：
评的人知道答案，而逐批严格度的漂移会伪装成组间差异。

还有一件事判定门本身回答不了：**它只能回答"在结构层面 DSL 有没有输"，
不能回答"该不该留"**——盲评清单里看不到布局表达能力（端口、样式、坐标、布局意图
只有一种格式能表达，放进去等于盖了组别的戳），而那恰恰是 DSL 存在的主要理由。
见 `conclusion.md` 第五节。

方案 Phase 1 清单里的 18 个 ID 至此都有对应文件。其中两个需要说明：

- **P1-18 是事后登记的**，状态 `done`。工程早在 Phase 0a 就建起来了，
  P1-15 又扩展了覆盖面，写文件只是把它登记回清单。
- **P1-09 / P1-17 的范围比方案原文大**：方案没有单列「文本映射到 IR」这一步，
  而 P1-08 / P1-16 都只做到语法树。不补上这一步，导入这条线没有产出物，
  下游的导出与对比评分也没有东西可评。理由写在各自 YAML 的 `scope_note` 里。

## DAG（Phase 2）

方案只给了 Phase 2 的任务**名字**（§13.6：Avalonia Canvas 基础渲染、动态阈值切换、
性能诊断面板、拖拽连线属性面板、变更高亮方案、用户测试、中级布局约束），
没有给 ID，也没有给依赖与工作量。所以 `P2-01` ~ `P2-13` 这套编号与切分是本仓定的，
依据是 §9（界面）、§10（性能）、§5（布局）、§11（错误呈现）里那些可验收的判据。

```
P2-01 绘制列表（纯数据 + 可注入文本度量）
  └── P2-02 画布控件与视口变换
        ├── P2-03 视口虚拟化与动态阈值切换
        ├── P2-04 性能诊断面板
        └── P2-05 属性面板
              └── P2-06 节点拖拽与固定
                    └── P2-07 连线与边的编辑
                          └── P2-08 变更高亮

P2-09 中级布局约束 ── P2-10 界面入口          （画布这条线之外，只需布局引擎）
P2-03 ── P2-11 布局失败处理与错误码呈现 ── P2-12 多窗口与多进程

P2-13 用户测试（装置与协议）── 依赖上面全部
```

**Phase 2 不等决策门 1。** 决策门 1 判的是 DSL 去留，那是文本格式之争；
而 Phase 2 做的是绘制列表、视口、剔除、面板、拖拽——这些都在 IR 之上，
换掉哪个文本格式都一样。唯一沾边的是 P2-09 / P2-10：布局意图现在只有 DSL
表达得出来，如果 DSL 被判掉，这两条要改成别的入口。所以这两条标了
`depends_on: P1-11/P1-12`（布局引擎与降级），没标决策门。

### Phase 2 DoD 落在哪些任务上

方案 §14.3 的 Phase 2 是七条，逐条对到任务：

| 判据（§14.3） | 落在 | 怎么量 |
|---|---|---|
| 1000 节点 FPS ≥ 30 | P2-03 | `DuetDiagram.App --benchmark-frames --nodes 1000 --frames 120`，判虚拟化那一档 |
| 渲染模式切换无卡顿 | P2-03 | 切换帧的耗时不超过稳态帧中位的 2 倍，且下一帧才生效；换档前后画布区域逐像素一致 |
| 虚拟化剔除率 > 80% | P2-03 | 同一条命令报出的剔除率（指令计数） |
| 拖拽延迟 < 16ms | P2-06 | 拖动期间每帧耗时采样，取 95 分位 |
| 属性面板切换 < 100ms | P2-05 | 切换选中元素到面板渲染完成的耗时 |
| 变更高亮通过用户测试 | P2-08 + P2-13 | 三种非颜色手段齐备（脉冲、角标、虚线轮廓）；P2-13 的协议里含这一项 |
| 中级约束正确 | P2-09 | 顺序约束与逐元素对齐的不变量测试 |

**P2-13 只交付装置与协议，不产出结论。** 用户测试要真人，报告在没有数据之前
不生成——和 `agreement.md` 同一个道理（见 `reports/compare-blind/README.md`）。
`tools/UserStudy` 三条命令（`verify` / `template` / `analyze`）能自动算的都算了：
量表、轮转拉丁方的顺序分配表、录入模板、四条统计判定与结论换算都在里面，
自检四十几条在构造好的已知答案上全通过。剩下的一步是**收十二到十五个人的数据**，
那一步编码 agent 做不了。所以 `reports/phase2-user-study.md` 现在不存在，这是有意的。

## DAG（Phase 3）

方案 §13.7 给了 Phase 3 的九项工作（ToolRegistry + 8 工具、MEAI 集成 + 上下文摘要、
错误回环、MCP Server stdio、MCP Server HTTP/SSE、乐观并发 + 软锁 + 广播、图层级权限、
Skill、高级布局约束），没有给 ID 与依赖。`P3-01` ~ `P3-16` 这套编号与切分是本仓定的，
依据是 §6（LLM 集成）、§7（MCP Server）、§8（Skill）、§14.3 与 §15.2 里那些可验收的判据。

```
P3-01 命令层：断边           ──┐
P3-02 命令层：布局与调色板   ──┼── P3-05 ToolRegistry 与八个工具
P3-03 命令层：组合与图层     ──┤          ├── P3-06 diagram_read 与上下文摘要
P3-04 命令层：页面、标签、动作 ┘          │     ├── P3-07 diagram_edit 与 diagram_style
                                          │     ├── P3-08 diagram_layout 与 diagram_composite
                                          │     └── P3-09 diagram_export / validate / undo_redo
                                          │                 └── P3-10 错误回环
                                          └── P3-11 MEAI 集成与内外部共用
                                                    └── P3-12 MCP Server stdio
                                                              ├── P3-13 MCP HTTP/SSE 与安全
                                                              │     └── P3-15 Skill 机制
                                                              └── P3-14 并发三层

P3-16 收尾：实机验收与多 agent 并发 ── 依赖上面全部
```

**四条命令层任务在最前面，因为它们是 P1-03 的遗留。** P1-03 的 `result.remaining_groups`
里记着节点、边、样式、布局、组合、图层、页面、调色板、标签、动作、文档十一组没做。
方案把命令层算在 Phase 1，而 Phase 3 的八个工具全靠它——不补上，`diagram_edit` 与
`diagram_style` 就只能是个空壳。放在 Phase 3 的第一位，是因为它是 Phase 3 的前置，
不是 Phase 1 的收尾。

**但这十一组不是十一条命令。** 起草第一版时照抄了命令清单里那张「计划中」表，
写成了十一条；对着代码核一遍才发现其中九条已经被 P1-05 的字段表吸收——
`set-node-field` 按字段名分发，label、shape、parent、layer、styleToken、style、text、
ports、richText、mathMode、desc、meta 都在它名下，边只有 label 与 style 两个。
判断标准是**被改的值挂在元素上还是挂在文档上**：挂在元素上的由字段表覆盖，
挂在文档子对象上的（`layout`、`palette`）才需要各立命令。
另外两条（`add-edge-waypoints`、`set-edge-route`）与 P2-07 的决定直接冲突：
折点写在 sidecar 的 `pinnedEdges`、不进 IR 也不走命令总线，所以它们不该有命令，
工具层也不该为折点开 action。

**第二轮核下来只剩一条。** 十一组里剩下 `reorder-node` 与 `disconnect-edge` 两条，
而 `reorder-node` 被查出**效果不可观测**：节点集合顺序在三个可能观察到它的地方都不可见——
结构哈希与外观哈希都先把节点按标识排序再遍历，所以次序进不了任何哈希；布局引擎在同层内
按横坐标排序、并列时用标识断掉，所以次序不改变任何坐标；绘制阶段确实按集合顺序出笔，
但布局保证同层不重叠，所以画面逐像素一致。做出来会是一条通过全部测试、却什么都没改变的命令。
它要成为真功能，前提是先给节点加一个 z 序字段并定义它在绘制里的语义，那是 IR 的改动。
所以 P3-01 只做 `disconnect-edge`。

这条比「查字段表」更值得记：**核到「效果有没有人能看见」才算核完**。字段表那一轮解决的是
「有没有别的路做同一件事」，这一轮解决的是「这件事做完了有没有区别」——两个问题都对着代码问，
而只有第二个能挡住一条自洽但无用的命令。

**P3-02 又核掉两条。** 布局那一组原本抄下来八条，其中 `pin-node` / `unpin-node` 站不住：
IR 里没有任何地方能存节点的绝对坐标（`NodeDef` 没有位置字段，`LayoutHints` 的四个列表
全是相对约束），而 DSL 的 `pin` 意图早就落到 sidecar 的 `pinnedNodes` 了。mapper 的注释
把理由写得很清楚——**坐标属于渲染结果**，存进 IR 会让同一份语义在不同机器上产生不同的
文档内容。命令层再立一条 pin 与这条原则直接冲突，同一个节点还会因此有两个固定位置。

这一条与 `reorder-node` 不同：那一条是「做完了没人看得出」，这一条是「做下去会破坏一条
已经定下的原则」。前者要等 IR 加字段，后者要等「坐标算不算文档内容」这个决定。
**两种都归到同一句：任务不能只对着计划核，也不能只对着代码核，还要对着已经定下的原则核。**

### Phase 3 DoD 落在哪些任务上

方案 §14.3 的 Phase 3 是六条，逐条对到任务：

| 判据（§14.3） | 落在 | 怎么量 |
|---|---|---|
| Codex CLI / Claude Code 可读写 | P3-16 | 真代理接上标准输入输出服务端读写一份文档 |
| 多 agent 并发无损坏 | P3-14 + P3-16 | 十个并发调用之后文档仍校验通过、版本单调递增 |
| 409 + diff | P3-14 | 冲突返回带差异，四种策略逐条对上 |
| 软锁 TTL | P3-14 | 拿锁、续期、无操作到期自动释放 |
| Skill 加载 | P3-15 | 两个 Skill 按 `skill://` 取得到，三层边界清楚 |
| 内外部工具行为一致 | P3-11 + P3-16 | 两侧名称、描述、schema 逐字相同；同一条命令的结果结构哈希相同 |

**P3-16 里有一半可能做不了。** 真实代理要装、要凭据、要有人看着跑，与 P1-14 的人工评分、
P2-13 的真人数据同一类。所以那一半在协议层用自建客户端验掉，真实代理那一半如果做不了
就照实记成「未验」，不写成「通过」——装置齐了不等于测过了，这一条在本仓已经踩过两次。

## 任务文件本身要能被解析

它们是给 agent 读的，读不了等于任务不存在。已经踩过两次：
P1-15 的 `expect` 值里有未加引号的冒号加空格，P2-12 的 `followups` 正文里有未加引号的引号。
两次都是整个文件解析失败，而**没有任何东西会报错**——任务就那么静默地消失了。

CI 里有一道门禁拦这件事，本地也可以先跑一遍：

```bash
python -c "import yaml,glob; [yaml.safe_load(open(f,encoding='utf-8')) for f in glob.glob('tasks/**/*.yaml',recursive=True)]; print('ok')"
```

注意以 `*` 开头的列表项会被当成 YAML 别名，以 `` ` `` 开头的会被当成非法记号，整段要加引号。
